# Real-time notifications – dependencies

Install these for Socket.IO–based real-time notifications:

- **Backend:** `socket.io`
  ```bash
  npm install socket.io
  ```

The frontend needs `socket.io-client` (see Frontend project).
